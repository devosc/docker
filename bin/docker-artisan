#!/usr/bin/env bash

if [ "$1" = "update-trusted-proxy" ]; then
    docker-app sed -i "s/APP_TRUSTED_PROXY=.*/APP_TRUSTED_PROXY=$(docker-traefik ip-address)/" .env
else
    docker-app php artisan "$@"
fi
