#!/usr/bin/env bash

source docker-build-env

if [ "down" = "$1" ]; then
    docker-compose --file $( dirname "${BASH_SOURCE[0]}" )/../mariadb/docker-compose.yml down --remove-orphans
else
    docker-compose --file $( dirname "${BASH_SOURCE[0]}" )/../mariadb/docker-compose.yml up -d
fi
