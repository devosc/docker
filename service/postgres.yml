version: '3.5'

volumes:
  postgres:

networks:
  public:
    external:
      name: traefik_webgateway

services:
  # Use postgres as the hostname in web apps.
  postgres:
    container_name: postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${DATABASE_ROOT_PASSWORD}
      TZ: ${TZ}
      PGTZ: ${TZ}
    image: postgres
    networks:
      - public
    restart: always
    volumes:
      - postgres:/var/lib/postgresql/data
